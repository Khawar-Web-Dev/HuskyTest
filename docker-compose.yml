version: '0.1'

services:
  my-react-app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "5173:5173"
    volumes:
      - ./src:/app/src
      - my-data:/app/data
    environment:
      - CHOKIDAR_USEPOLLING=true

volumes:
  my-data: